# Environment Variables for Vercel Deployment

# Add these in Vercel Dashboard > Project Settings > Environment Variables

# Database (you'll need to set up a production database first)
DATABASE_URL="your-production-database-url-here"

# JWT Secret (generate a random 32+ character string)
JWT_SECRET="your-super-secret-jwt-key-at-least-32-characters-long"

# Next.js URL (will be provided by Vercel after first deploy)
NEXTAUTH_URL="https://your-app-name.vercel.app"

# Another random secret for NextAuth
NEXTAUTH_SECRET="another-random-secret-key-32-characters-plus"

# Email Configuration (Optional - without this, emails will be logged to console)
# You can use Gmail, SendGrid, Amazon SES, or any SMTP provider
EMAIL_SERVER_HOST="smtp.gmail.com"
EMAIL_SERVER_PORT="587"
EMAIL_SERVER_USER="your-email@gmail.com"
EMAIL_SERVER_PASSWORD="your-app-password"
EMAIL_FROM="noreply@yourdomain.com"

# APP_URL (used for email links - same as NEXTAUTH_URL usually)
APP_URL="https://your-app-name.vercel.app"

# Note: You can generate random secrets using:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# Note: For Gmail, you'll need to:
# 1. Enable 2-factor authentication
# 2. Generate an "App Password" (not your regular password)
# 3. Use the app password as EMAIL_SERVER_PASSWORD
